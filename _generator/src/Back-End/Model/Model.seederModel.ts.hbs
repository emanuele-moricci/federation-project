import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();

async function seed{{ capital ModelName }}s() {
  await prisma.{{ firstLower ModelName }}.create({
    data: { },
  });
}

export default seed{{ capital ModelName }}s;
