overwrite: true
schema: 'http://localhost:4002/graphql'
generates:
  src/graphql/generated/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      contextType: src/config/prismaConfig#IPrismaContext
      useIndexSignature: true
      federation: true
