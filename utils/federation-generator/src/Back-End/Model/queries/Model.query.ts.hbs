import { GraphQLFieldConfig, GraphQLFieldResolver, GraphQLList } from 'graphql';
import { IApolloServerContext } from '@src/lib/interfaces/IApolloServerContext';

import { {{ properCase ModelName }} } from '@prisma/client'; // ADD THE PRISMA MODEL AND MIGRATE
import {{ properCase ModelName }}Type from '@src/graphql/schema/Models/{{ properCase ModelName }}/{{ properCase ModelName }}';

import * as service from '@src/services/{{ firstLower ModelName }}Service';

/**
*
* Query getAll{{ properCase ModelName }}s
*
*/
export const getAll{{ properCase ModelName }}sResolver: GraphQLFieldResolver<
  unknown,
  IApolloServerContext
> = async (_source, _args, _context, _info): Promise<{{ properCase ModelName }}[]> => {
  // ADD YOUR LOGIC HERE
}

const getAll{{ properCase ModelName }}sQuery: GraphQLFieldConfig<unknown, IApolloServerContext> = {
  description: 'Get all {{ properCase ModelName }}s query',
  type: GraphQLList({{ properCase ModelName }}Type),
  resolve: getAll{{ properCase ModelName }}sResolver,
};

export default getAll{{ properCase ModelName }}sQuery;
