import {
  GraphQLFieldConfig,
  GraphQLFieldResolver,
  GraphQLInputObjectType,
} from 'graphql';
import { IApolloServerContext } from '@config/apollo/IApolloServerContext';

import { {{ properCase ModelName }} } from '@prisma/client'; // ADD THE PRISMA MODEL AND MIGRATE
import {{ properCase ModelName }}Type from '@schema/Models/{{ properCase ModelName }}/{{ properCase ModelName }}';

import * as service from '@services/{{ firstLower ModelName }}Service';

/**
*
* Mutation create{{ properCase ModelName }}
*
*/
export const create{{ properCase ModelName }}Resolver: GraphQLFieldResolver<
  unknown,
  IApolloServerContext
> = async (
  _source,
  { input: { /* ADD YOUR INPUT PROPERTIES HERE */ } },
  _context,
  _info
): Promise<{{ properCase ModelName }}> => {
  // ADD YOUR LOGIC HERE
};

export const Create{{ properCase ModelName }}Input: GraphQLInputObjectType = new GraphQLInputObjectType(
  {
    name: 'Create{{ properCase ModelName }}Input',
    description: 'Create {{ properCase ModelName }} input',
    fields: { /* ADD YOUR INPUT PROPERTIES HERE */ },
  }
);

const create{{ properCase ModelName }}Mutation: GraphQLFieldConfig<unknown, IApolloServerContext> = {
  description: 'Create {{ properCase ModelName }}',
  type: {{ properCase ModelName }}Type,
  args: {
    input: {
      type: Create{{ properCase ModelName }}Input,
    },
  },
  resolve: create{{ properCase ModelName }}Resolver,
};

export default create{{ properCase ModelName }}Mutation;
